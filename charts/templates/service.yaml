apiVersion: v1
kind: Service
metadata:
  name: {{ include "log-ingestion.fullname" . }}
  labels:
    {{- include "log-ingestion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "log-ingestion.selectorLabels" . | nindent 4 }}
